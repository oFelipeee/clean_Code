const { Router } = require("express");
const router = Router();

router.post('/', );

router.put('/:id', );

router.delete('/:id', );

router.get('/:id', );

router.get('/:id', );

module.exports = router;