const { Router } = require("express");
const router = Router();

// Funcao de criar
router.post('/', );

// Funcao de editar
router.put('/:id', );

// Funcao de deletor
router.delete('/:id', );

// Funcao de buscar unico
router.get('/:id', );

// Funcao de buscar todos
router.get('/:id', );

module.exports = router;